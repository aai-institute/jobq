build:
  base_image: python:3.11-slim
  dependencies:
    apt: ~
    pip: [-r requirements.txt, -e .]
  workdir: /app
  filesystem:
    copy:
      - . : .
  config:
    env:
      # For Python imports from the project root dir
      - PYTHONPATH: /app
