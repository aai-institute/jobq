build:
  base_image: rayproject/ray:2.11.0-py311 # python:3.11-slim
  dependencies:
    apt:
    pip: [-r requirements.txt, .]
  workdir: /home/ray/app
  config:
    env:
      # For Python imports from the project root dir
      - PYTHONPATH: /home/ray/app
  user:
    uid: 1000
    gid: 1000
