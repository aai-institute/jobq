apiVersion: v1
kind: Service
metadata:
  name: {{ include "jobs-server.fullname" . }}
  labels:
    {{- include "jobs-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jobs-server.selectorLabels" . | nindent 4 }}
